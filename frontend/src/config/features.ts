export const FEATURE_AI_AGENT = true as const;

// Flip to true to re-enable the Ordinay Intelligence (Agent) feature
// and update electron-builder.json to include backend/src/agent in production builds.
