import type { ReactNode } from "react";

export const DEFAULT_SETTINGS: Record<string, unknown>;
export function SettingsProvider(props: { children: ReactNode }): JSX.Element;
export function useSettings(): unknown;
